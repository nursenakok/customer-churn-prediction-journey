# customer-churn-prediction-journey
Customer churn prediction using logistic regression on two datasets. The first dataset reveals a real-world data drift problem (89%→58% accuracy drop), while the second demonstrates a production-ready solution (74% accuracy, 0.84 ROC-AUC). 
